import type { NextPage } from 'next';
import NavBar from '../components/basic/NavBar';

const Home: NextPage = () => {
  return (
    <main>
      <NavBar />
    </main>
  );
};

export default Home;
